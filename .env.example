# Example environment variables for the frontend
# Set the backend API base URL (include protocol and port if needed)
VITE_API_BASE_URL=http://localhost:9090
# Optional WebSocket base URL (defaults to VITE_API_BASE_URL)
VITE_WS_URL=http://localhost:9090
